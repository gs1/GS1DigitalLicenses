{
  "name": "gs1-credential-test",
  "version": "1.0.0",
  "description": "Test suite for GS1 credential schemas and templates",
  "type": "module",
  "scripts": {
    "test": "npm run test:templates && npm run test:schema",
    "test:templates": "node test-templates.mjs",
    "test:schema": "node test-schema.mjs"
  },
  "dependencies": {
    "mustache": "^4.2.0",
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  },
  "devDependencies": {}
}

